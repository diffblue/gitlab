- if @project.licensed_feature_available?(:external_status_checks) && Feature.enabled?(:only_allow_merge_if_all_status_checks_passed, @project)
  = form.gitlab_ui_checkbox_component :only_allow_merge_if_all_status_checks_passed,
    s_('ProjectSettings|Status checks must succeed'),
    help_text: s_("Merge requests can't be merged if the status checks did not succeed or are still running.")
