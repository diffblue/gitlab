= s_("in %{duration}, using %{compute_credits} compute credits, and was queued for %{queued_duration}").html_safe % { duration: duration, compute_credits: pipeline.total_ci_minutes_consumed, queued_duration: queued_duration }
