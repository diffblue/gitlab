<% if diff_summary_available?(@merge_request, @previous_reviewers, @recipient) %>
<%= s_('Notify|%{name} requested a new review on %{mr_link}.') % {name: sanitize_name(@updated_by_user.name), mr_link: merge_request_reference_link(@merge_request)} %>
<%= render 'notify/diff_summary' %>
<% end %>
