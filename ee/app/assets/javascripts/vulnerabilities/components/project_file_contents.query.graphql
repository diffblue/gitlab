query vulnerabilityFileContents($fullPath: ID!, $filePath: String!) {
  project(fullPath: $fullPath) {
    id
    repository {
      blobs(paths: [$filePath]) {
        nodes {
          id
          rawBlob
        }
      }
    }
  }
}
