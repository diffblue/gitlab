query getAddOnPurchase($fullPath: ID!, $addOnName: String!) {
  namespace(fullPath: $fullPath) {
    id
    addOnPurchase(addOnName: $addOnName) {
      id
      name
      assignedQuantity
      purchasedQuantity
    }
  }
}
