query getGroupCiMinutesUsage($namespaceId: NamespaceID) {
  ciMinutesUsage(namespaceId: $namespaceId) {
    nodes {
      month
      monthIso8601
      minutes
      sharedRunnersDuration
    }
  }
}
