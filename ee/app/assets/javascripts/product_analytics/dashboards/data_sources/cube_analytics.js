export const fetch = async (query) => {
  // TODO: Call the cube library to fetch data from the product analytics proxy.
  // Will be added with https://gitlab.com/gitlab-org/gitlab/-/issues/377708
  return query;
};
