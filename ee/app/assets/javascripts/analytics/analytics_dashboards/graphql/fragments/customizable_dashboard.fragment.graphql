fragment CustomizableDashboardFragment on CustomizableDashboard {
  slug
  title
  description
  userDefined
  panels {
    nodes {
      title
      gridAttributes
      queryOverrides
      visualization {
        slug
        type
        options
        data
        errors
      }
    }
  }
}
