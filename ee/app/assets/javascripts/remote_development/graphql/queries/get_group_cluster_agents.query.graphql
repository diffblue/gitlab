query getGroupClusterAgents($groupPath: ID!) {
  group(fullPath: $groupPath) {
    id
    clusterAgents {
      nodes {
        id
        name
        project {
          id
          nameWithNamespace
        }
      }
    }
  }
}
